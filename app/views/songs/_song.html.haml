.song
  %h1= link_to song.title, song_path(song)
  .result-info
    %p
      = t("songs.show.by") \
      = link_to song.artist.full_name, artist_path(song.artist)
    %p
      = t("songs.show.from") \
      = link_to song.album.title, album_path(song.album)
